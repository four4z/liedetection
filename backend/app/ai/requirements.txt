# --- Video / Audio processing ---
moviepy==1.0.3

silero-vad
facenet-pytorch

# Pin all three PyTorch packages to the same version to prevent binary ABI mismatch.
# torchaudio X.Y.Z must match torch X.Y.Z exactly — mismatched versions cause
# WinError 127 ("The specified procedure could not be found") at import time.
torch==2.2.2
torchaudio==2.2.2
torchvision==0.17.2

torchcodec

soundfile
pysoundfile

Pillow

requests